import Navbar from '../Navbar/Navbar';

const Header = () => {
    return (
        <div>
            <div>
                <Navbar></Navbar>
            </div>
        </div>
    );
};

export default Header;